/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ConsultarViajes } from './consultar-viajes';

describe('ConsultarViajes', () => {
  let component: ConsultarViajes;
  let fixture: ComponentFixture<ConsultarViajes>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [ConsultarViajes]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ConsultarViajes);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/