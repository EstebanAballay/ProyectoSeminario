services:
  users:
    build: ./apps/users
    command: npm run start:dev
    container_name: users-service   # nombre del contenedor
    env_file: .env                  # variables de entorno
    ports:
      - "3003:3003"                # host:contenedor
    networks:
      - backend-network
    volumes:
      - ./apps/users:/usr/src/app
      - /usr/src/app/node_modules
    restart: always                 # reinicia si falla

  cobro:
    build: ./apps/cobro
    container_name: cobro-service
    command: npm run start:dev
    env_file: .env
    ports:
      - "3001:3001"
    networks:
      - backend-network
    volumes:
      - ./apps/cobro:/app
      - /app/node_modules
    restart: always
    depends_on:                      # espera que 'users' se inicie primero
      - users

  unidad:
    build: ./apps/unidad
    container_name: unidad-service
    command: npm run start:dev
    env_file: .env
    ports:
      - "3002:3002"
    networks:
      - backend-network
    volumes:
      - ./apps/unidad:/app
      - /app/node_modules
    restart: always

  viaje:
    build: ./apps/viaje
    container_name: viaje-service
    command: npm run start:dev
    env_file: .env
    ports:
      - "3004:3004"
    networks:
      - backend-network
    volumes:
      - ./apps/viaje:/app
      - /app/node_modules
    restart: always
    depends_on:
      - users
      - unidad
  auth:
    build:
      context: .
      dockerfile: apps/auth/Dockerfile
    container_name: auth-service
    command: npm run start:dev
    env_file: .env
    ports:
      - "3005:3005"
    networks:
      - backend-network
    volumes:
      - ./apps/auth:/app
      - /app/node_modules
    restart: always

networks:
  backend-network:
    driver: bridge
